# Validação formal

O protocolo `iq-oq-pq-matrix.yml` separa instalação (IQ), operação (OQ) e
performance/usabilidade (PQ). Execute `python manage.py check_validation_protocol
--fail-on-error` antes da revisão por QA. Cada protocolo deve receber evidência,
aprovador e data em procedimento controlado.
